bind 0.0.0.0
protected-mode no

# Memory cap — stays within Pi 5 budget
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence: light RDB snapshots, no AOF (entity state is ephemeral)
save 900 1
save 300 10
appendonly no

loglevel notice
databases 1
